[project]
name = "demo"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "akshare",
    "pandas",
    "talib",
    "langchain",
    "langchain_openai",
    "langchain_core",]
